; Keep PHP-FPM in foreground so the container stays alive.
[global]
daemonize = no

; Override [www] pool so PHP-FPM listens on all interfaces.
; Required when nginx runs in a separate container (fastcgi_pass app:9000).
[www]
listen = 0.0.0.0:9000
